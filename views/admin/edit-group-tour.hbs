{{!-- <form> 裡面的 action 不支援 PUT 跟 DELETE，需使用 _method --}}
  {{!-- 表單發出的 POST 請求且包含 <input type="file">，設定資料格式 Content-Type(enctype) 改成 multipart/form-data --}}
<form action="/admin/group-tours/{{groupTour.id}}?_method=PUT" method="POST" enctype="multipart/form-data">
  <div class="mb-4 text-center">
    <h1 class="h3 font-weight-normal mb-3">Edit Group Tour</h1>
  </div>

  {{> admin-group-tour-form}}

</form>